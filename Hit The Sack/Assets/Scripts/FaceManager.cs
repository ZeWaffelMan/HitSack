using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FaceManager : MonoBehaviour
{
    /*
    public GameObject[] faces;
    private int facesIndex;

    Smacking smacking;

    private void Awake()
    {
        smacking = GameObject.Find("MouseManager").GetComponent<Smacking>();
    }

    private void Update()
    {
        for (int i = 0; i < faces.Length; i++)
        {
            if(i == facesIndex)
            {
                faces[i].SetActive(true);
            }
            else
            {
                faces[i].SetActive(false);
            }
        }

        // If you hit the sack, he looks hurt
        if (facesIndex == 0 && Smacking.hit == true)
        {
            facesIndex = 1;
        }else if (facesIndex == 1 && Smacking.hit == false)
        {
            facesIndex = 0;
        }
    }

    */
}
